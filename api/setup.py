from setuptools import setup, find_packages
from os import path
from io import open

setup(
    name="api",  # Required
    version="1.0.1",  # Required
    description="p league stats mining",  # Optional
    # packages=find_packages(exclude=["importlib", "pymysql", "pandas"]),
)
